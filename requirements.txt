# 核心
chromadb>=0.5.0
sentence-transformers>=3.0.0
sqlite-utils>=3.36

# LLM（本地 Ollama 或 OpenAI 兼容 API）
openai>=1.40.0
httpx>=0.27.0

# 工具
pyyaml>=6.0
python-dateutil>=2.9.0
rich>=13.7.0          # 终端美化输出
python-dotenv>=1.0.0  # 从 .env 读取 OPENROUTER_API_KEY
jieba>=0.42.1         # 中文分词 + 词性（实体抽取）
psycopg[binary]>=3.1  # 可选：词表/停用词 PostgreSQL 后端

# Web UI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
