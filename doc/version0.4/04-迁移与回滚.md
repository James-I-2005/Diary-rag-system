# v0.4 迁移与回滚

## 迁移

1. 拉取代码
2. `python main.py sentences`（或先 `--ratio 0.05` 试跑）
3. `python main.py index`
4. 确认 `SELECT COUNT(*) FROM rag_sentences` ≈ Chroma `diary_sentences.count()`
5. 冒烟：`python -m src.query`，检查召回 id 形如 `*_s*`

## 废弃清单（可暂留数据）

- 表 `memory_views`（不再写入）
- Chroma `diary_views`、`diary_chunks`（主路径不再写入 chunks）
- `src/engine/operators/view.py`、`src/memory_extraction/`、`src/memory_views.py`

## 回滚

依赖 git 回退；本阶段不做 `RETRIEVAL_UNIT=chunk` 开关。
